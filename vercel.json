{
  "buildCommand": "rm -f index.html && npm install --verbose && npm run build --verbose",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install",
  "devCommand": "npm run dev"
}
